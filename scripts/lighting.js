HTomb = (function(HTomb) {
  "use strict";
  var coord = HTomb.Utils.coord;
  //validate for lighting?
  //okay...lights and lighting...
  //if we really wanted to we could use their inverse shadowcasting thing
  //we could also use whatever the fastest algorithm is
  // do a really optimized one
  //maybe the entire ceiling is made of lights?
  //this is probably too hard to do sitting down
  return HTomb;
})(HTomb);
