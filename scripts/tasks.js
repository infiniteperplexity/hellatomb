HTomb = (function(HTomb) {
  "use strict";

//  var b = HTomb.Behavior;

  HTomb.Entity.define({
      template: "DigTask",
      name: "digging task",
      isZone: true,
      bg: "brown"
  });

  return HTomb;
})(HTomb);
